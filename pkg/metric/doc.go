// Package metric contains the Prometheus collectors used by BlockQueue's queue
// and worker runtimes. Most applications should configure metrics through
// blockqueue.Options and worker.Options instead of registering collectors
// directly.
package metric
